data = {'color':'red', 'fruit':'apple', 'pet':'dog', 'car':'larmboghini'}

#for x, y in data.items():
    #print(x, y)

#for x, y in thisdict.items():
  #print(x, y)

for key in data:
    value = data[key]
    print(key, + ':' str[value])